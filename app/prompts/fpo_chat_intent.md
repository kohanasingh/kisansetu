<!-- Versioned prompt: fpo_chat_intent. Loaded via
llm.load_prompt("fpo_chat_intent").

Classifies an FPO staff chatbot message for orchestrator/fpo_router.py —
advisory / climate / ingestion-status / general — using GPT-4o JSON-mode.
Simpler than farmer_intent: no identity resolution precedes it. -->
